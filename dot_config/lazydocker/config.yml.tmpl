{{- $t := index .themes .theme -}}
gui:
  theme:
    # Border color of focused panel
    activeBorderColor:
      - "{{ $t.border_active }}"
      - bold

    # Border color of non-focused panels
    inactiveBorderColor:
      - "{{ $t.border_inactive }}"

    # Color of keybindings help text in the bottom line
    optionsTextColor:
      - "{{ $t.options_text }}"

    # Background color of selected line
    selectedLineBgColor:
      - "{{ $t.selected_bg }}"

    # Background color of selected line when view doesn't have focus
    inactiveViewSelectedLineBgColor:
      - "{{ $t.inactive_selected_bg }}"
